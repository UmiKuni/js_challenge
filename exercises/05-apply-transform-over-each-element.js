/**
 * Problem 5: Apply Transform Over Each Element in Array (Map)
 *
 * Given an integer array arr and a mapping function fn, return a new array with a transformation
 * applied to each element.
 *
 * The returned array should be created such that returnedArray[i] = fn(arr[i], i).
 *
 * Please solve it without using the built-in Array.map method.
 *
 * Example 1:
 * Input: arr = [1,2,3], fn = function plusone(n) { return n + 1; }
 * Output: [2,3,4]
 *
 * Example 2:
 * Input: arr = [1,2,3], fn = function plusI(n, i) { return n + i; }
 * Output: [1,3,5]
 *
 * Constraints:
 * - 0 <= arr.length <= 1000
 * - -10^9 <= arr[i] <= 10^9
 */

function map(arr, fn) {
  // TODO: Implement this function
}

module.exports = map;
